Fullstack проект с **backend на Node.js + TypeScript + Express**, настроенным для использования **Bun**, **Husky**, **Prettier** и **lint-staged**.

---

## Структура проекта

```
pingpals-fullstack/
├─ .git/
├─ .husky/           # Git hooks (pre-commit)
├─ node_modules/     # Общие зависимости
├─ package.json      # Корневой package.json (монорепо)
├─ bun.lockb         # Bun lock файл
├─ .prettierrc
├─ .prettierignore
├─ .gitignore
├─ backend/
│  ├─ src/
│  ├─ tsconfig.json
│  ├─ package.json
│  └─ .env
└─ frontend/          # Будущий frontend
   ├─ src/
   └─ package.json
```

---

## Установка

В корне проекта:

```
bun install
```

Все зависимости для dev (**Husky**, **lint-staged**, **Prettier**, **TypeScript**) находятся в корневом `package.json`.

---

## Скрипты

1. **Форматирование кода**

   ```
   bun run format
   ```

   Форматирует все файлы в `backend` и `frontend` по `.prettierrc`.

2. **Запуск сервисов**

   ```
   bun run start:backend   # Backend только
   bun run start:frontend  # Frontend только
   bun run start           # Оба сервиса одновременно
   ```

   Используется `concurrently` для параллельного запуска.

3. **Разработка backend**

   В папке `backend` доступны скрипты:

   ```
   bun run dev    # Запуск backend с nodemon и ts-node
   bun run build  # Компиляция TypeScript
   bun run start  # Запуск скомпилированного кода
   ```

---

## Git Hooks

Используется **Husky** с `pre-commit` хуком.  
Все staged файлы в backend и frontend проходят через Prettier перед коммитом:

```
git add .
git commit -m "your commit message"
```

Никакие несформатированные файлы не попадут в коммит.

---

## Конфигурации

- `.prettierrc` — правила **Prettier**.
- `.prettierignore` — файлы и папки, которые Prettier игнорирует.
- `.gitignore` — игнорируемые файлы и папки Git.
- `tsconfig.json` в `backend` — настройки **TypeScript** для backend.

---

## 📌 Примечания

- Dev-зависимости находятся в корневом `package.json`, чтобы все подпроекты использовали одинаковые версии инструментов.
- Runtime-зависимости (`express`, `dotenv` и т.д.) находятся в `package.json` соответствующего пакета (`backend` или `frontend`).
- В будущем фронтенд может быть подключен аналогично backend.
